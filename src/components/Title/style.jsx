import styled from 'styled-components';
import { theme } from '../../theme';

export const TitleText = styled.h2`
    text-align: center;
    color: ${ theme.text };
    letter-spacing: 1px;
    font-weight: 300;
    font-size: 28px;
`;