import styled from 'styled-components';

export const ContainerFotos = styled.div`
    width: 80%;
    margin: auto;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
`;